# Copy thành .env và điền giá trị
# Backend API (Tin tức & Sự kiện, auth, ...)
VITE_API_BASE_URL=https://narrow-cleopatra-phucadmin-52a8db7b.koyeb.app/api

# (Tùy chọn) Token admin để chạy seed Tin tức/Sự kiện: npm run seed:events-news
# Lấy token sau khi đăng nhập admin (localStorage / cookie), không commit .env
# ADMIN_TOKEN=eyJhbGciOiJIUzI1NiIs...
